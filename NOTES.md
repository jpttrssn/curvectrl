# NOTES — reference, working state & decisions

Reference for continuing work on the roll library (rolls ↔ frame grid ↔ detail view navigation), thumbnails, and the GPU detail pipeline.

## Current state

### File layout

- `src/app.rs` — UI (tiles, detail crossfade, editing panel), state + messages, the async decode/export pumps.
- `src/pipeline.rs` — the RAW-to-image pipeline shared by every CPU bake: sensor decode (`decode_raw_detail`), mono reconstruction (`flatten_bayer`, `downsample_thumbnail`), tone shaping (`bake_tone`/`render_tail`/`pivots_for`), and geometry (`crop_rgba`/`rotate_quarters`/`orient`), plus `DetailDecode`.
- `src/shader.rs` — `DetailProgram` implementing `iced::widget::shader::{Program, Primitive, Pipeline}` for the detail view; the shared tone model lives here (`tone_model`, `curve_remap`, `apply_curve`).
- `src/edit_manifest.rs` — per-roll edit persistence: `RollManifest`/`EditData`/`ToneEdit` (serde+toml), the `.film-roll.toml` sidecar reader/writer (atomic temp+rename), `reconcile`, unit tests.
- `src/exif_writer.rs` — EXIF writing for exports: `shifted_datetime` (ISO date + seconds), the hand-rolled TIFF blob (`build_tiff`), and the JPEG APP1 splice (`jpeg_app1`/`splice_after_soi`); PNG's `eXIf` is written via the `png` crate directly. No new crates; its unit tests round-trip through `exif` (kamadak-exif).
- `src/export.rs` — the export path: `ExportPreset`/`ExportFormat`/`ExportSize`/`ExportOptions` (the dialog's `options_for_choice`/`export_format_choice`/`export_overwrite_choice`), file naming (`export_name`, `dated_export_name`, `roll_hash`, `temp_export_path`, `export_fraction`), `is_export_artifact` (keeps exports out of the frame grid), and the encoders `export_frames`/`export_one`/`export_jpeg`/`export_png`.
- `src/library.rs` — roll-list logic: ISO date helpers (`parse_iso_date`, `valid_iso_date`, `roll_dates_valid`, `format_roll_card_dates`), the date-aware search (`MonthAliases`, `parse_search_query`, `roll_matches_terms`, `filtered_rolls`, `roll_date_cmp`), the unified grid cells + arrow-key nav (`LibraryCell`, `library_cells`, `library_cell_index`, `nav_target`, `paginate`), and the roll-metadata manifest setters (`record_roll_*`).
- `src/config.rs` — persisted app config (cosmic-config derive, schema v1): the roll list `rolls: Vec<String>` (absolute roll directories).
- `src/shader/exposure.wgsl` — fullscreen-triangle WGSL that samples a linear mono `R16Float` texture, applies the sensor-space `2^EV` gain (for an inverted/film preset the gain is `2^-EV` and the fragment ALSO density-inverts the transmission first — `curve(invert(s·2^-EV))`), re-shapes the positive via the CPU-built 2048×1 tone LUT (`t_tone`, sampled on `t = p^(1/2.2)` and divided by the 512× pre-scale — the tone model lives only in Rust `tone_model`, so the WGSL never changes for tone work), sRGB-encodes, and maps fragments through a zoom/pan transform (`view_uv`).
- `src/film.rs` — density-space mono inversion + base measurement (`MonoStock`, `FILM_CHOICES`, `ACTIVE_STOCK` = Ilford HP5+).
- `src/detail_area.rs` — `DetailArea` widget: an iced-`MouseArea` clone that reports cursor positions relative to the widget **center** (needed by the zoom-anchor math), fires `on_release` when the cursor leaves while pressed (iced suppresses release outside bounds), and stays stateless (`tree::State::None`). A11y nodes omitted (the trait default covers it; no direct `iced_accessibility` dep).

### Current behavior

- **Library page**: the configured film rolls (config key `rolls`, a `Vec<String>` of absolute directories) are listed as cover cards in a responsive grid, **date-sorted** — undated first in name order, then dated rolls newest-start-date first (pure `roll_date_cmp`, applied at the single `library_cells` choke point shared by grid render, arrow-key navigation, and pre-select; the backing `self.rolls` order stays name-sorted). There is no library toolbar: the only header control is the search (see Search). The library **pre-selects** the first visible roll whenever nothing is selected (`select_first_visible_roll`, no-op while a roll is open) so it never idles un-highlighted; a freshly added roll skips this and drills straight into its frame grid with the selection pinned for backing out. Each card is one interactive unit (`MouseArea`: `RollSelected` on press, `RollActivated`/Enter drills in via `open_roll`): a full-bleed cover, the display name, and a caption row with the frame count plus (dated rolls) the human-readable date range. The cover is flush to the card's top edge with `content_fit(ContentFit::Cover)` + a **top-only** `border_radius(...bottom(radius))`: the pinned iced-wgpu image shader clips to a rounded-box SDF whose radius is rotated 180° from `border::Radius`'s struct order (first two entries render on the bottom), so the top clip is expressed via `.bottom()`; `radius` comes from `cosmic::theme::active().cosmic().corner_radii.radius_s[0]`, matching the COSMIC system Roundness setting exactly. **Selection is an overlay, not a card style**: the card sits in a `Stack` on a `Container::Primary` surface, and a selected card gets a transparent `Container::custom` accent ring (2.0, `radius_s`) on top, so the full-bleed cover can never hide the highlight (iced paints a container border *behind* its children); the ring is decorative (no `MouseArea`). Empty states: a truly empty library shows **no hint** (the Add Roll tile alone is self-explanatory); the `no-rolls-found` overlay survives only for the search-mismatch case.
- **Add Roll tile & remove roll**: an **Add Roll** tile (a `Container::Primary` card via the shared `selectable_tile` wrapper, same selection ring) is always the first grid cell while **no search is active** — `library_cells` prepends it purely on `query.trim().is_empty()`, so a non-blank search hides it. A single click selects it; double-click (or Enter) opens the system folder picker (`cosmic::dialog::file_chooser::open::Dialog::open_folder()` via `open_roll_picker()`/`Message::AddRoll`). Re-adding an existing roll re-scans it (the tile doubles as manual refresh); **File → Add roll** is the inline-picker alternative mid-search. `RollAdded(dir)` works from anywhere (the roll persists app-wide), pins the new roll in the library selection, and drills straight into its frame grid with the first frame pre-selected; backing out with Esc returns to the library with it highlighted. **Remove roll is non-destructive**: the library roll-drawer button / File → Remove roll (gated to the rolls view with a roll selected) drops the dir from the persisted config (`rolls.retain` + `persist_config`), clears a stale selection + closes the roll-info drawer, and never touches the files or `.film-roll.toml` — reversible via Add roll.
- **Roll metadata (name, dates, film, base)**: the roll-info context drawer (`ContextPage::RollInfo`, the Space/Details drawer target on the library page — needs a `LibrarySelection::Roll`, never opens while a roll is open) lists the editable **name** (heading text field, commit on Enter; `None` falls back to the directory leaf; library card title + search + sort tiebreak all read it), the full path, the **Film preset** dropdown (`None → Generic → eight stocks alphabetically`: Fomapan 100, Fomapan 400, Ilford Delta 400, Ilford FP4 Plus, Ilford HP5+, Kentmere 400, Kodak T-Max 400, Kodak Tri-X 400 — `FILM_CHOICES` is the single source; `Generic` is a neutral B&W fallback) and, only while a film is chosen, a **Base** dropdown (**Preset base → Auto per frame → Auto selected frame**), the **Dates** fields (start + optional end, ISO `YYYY-MM-DD`, Enter to commit; validation via `valid_iso_date`, `end ≥ start` enforced by `roll_dates_valid`), frame count, and cover file name (or `no-cover`). The card date caption is human-readable with localized short months (`format_roll_card_dates`; e.g. `May 30 - Jun 2 2026`), falling back to the raw ISO range on malformed input. Film choice and base strategy are two orthogonal roll-level axes: `BaseMode` maps to `BaseConfig` exactly — `Preset`→`{calibrated:None, auto:false}`, `AutoPerFrame`→`{calibrated:None, auto:true}`, `AutoSelectedFrame`→`{calibrated, auto:false}` (pins the roll to one designated calibration frame — default = the first sorted frame, auto-measured on base-mode selection and roll open; **Edit → Calibrate from this frame** re-designates it; a small theme-accent indicator dot pinned to the tile's bottom-right corner marks the designated frame). The manifest stores the designated frame (`calibration_frame: Option<String>`) and its measured transmission (`base: Option<f32>`); the old **Edit → Auto base per frame / Use preset base** items are gone. See `raw_ev.md` part 3.
- **Search (library-only, debounced, date-aware)**: the header search applies only to the roll library — an open roll's frame grid always shows every file, and the header search control is hidden entirely while a roll is open. The control mirrors cosmic-files' `header_end`: inactive → `system-search-symbolic` icon button (`.tooltip("Search")`, Ctrl+F also activates); active → a fixed-width (240) `search_input` that hides back to the icon once cleared. Typing is **debounced** (trailing edge): `search` is the live typed draft, `search_filter` the applied term; each keystroke bumps a monotonic `search_version` and spawns a 300 ms `SEARCH_DEBOUNCE_MS` task emitting `SearchCommitted(term, serial)`, applied only when `serial == search_version` (so a stale commit can't resurrect a clear/escape). Matching covers names **and dates**: whitespace-separated tokens are classified by `parse_search_query` into `Text` / `Date{year}` / `Date{month}` and ANDed by `roll_matches_terms` — `2026`, `may`, `May 2026`, or `chi 2026` all work; a `Date` term also matches the literal token in the roll name. Month names resolve through `MonthAliases` (localized `month-01`…`month-12` + the always-English full/3-letter set); ISO parsing is single-sourced in `parse_iso_date` (`valid_iso_date` and `format_roll_card_dates` delegate to it). Escape on the library page deactivates the search first; inside a roll it leaves the term alone (persists and reapplies on return).
- **Frame grid**: an open roll's grid lists every regular non-dot file in the roll directory as tiles, sorted by name (dotfiles — including the edit manifest or its temp — never become tiles); there is no frame search. Columns adapt to window width via `Grid::fluid(THUMB_SIZE)` + `.height(Sizing::AspectRatio(TILE_ASPECT))` inside a scrollable that wraps the grid in `widget::container(grid).width(Length::Fill).padding(space_s)` (the 16 px inset — this iced fork's `Scrollable` has no `.padding()` builder). Cells never exceed `THUMB_SIZE` px and stay square. Each tile is **image-only** (the filename caption was dropped — the name lives in the frame-info drawer): a single `MouseArea` (`FrameSelected` / `ThumbnailActivated` double-click) wraps the image + a `space_s`-padded name slot on a `Container::Primary` surface, with the same shared `selection_ring()` overlay as the library when `frame_selected` matches; `ContentFit::Contain` (never cropped, so a film frame's full framing stays visible); `image-loading-symbolic` / `image-missing-symbolic` icons for pending/failed. Opening a roll pre-selects its first visible frame (highlight/latch only — the detail view stays closed and Enter opens it).
- **Frame-info drawer**: on the grid with a highlighted frame, Space/View→Details opens `ContextPage::FrameInfo` showing the frame's name, dimensions, and basic EXIF — camera (make+model), ISO, shutter speed, aperture, focal length, lens, capture date — parsed via `kamadak-exif` on demand (`load_frame_meta`, results cached per `Tile`, `frame_meta_inflight` de-dupes) with a quiet "No metadata available" hint on unparseable files. When the roll has a start date it leads with an **Original date** row — `start_date` + the frame's full-roll position in seconds via `shifted_datetime`, i.e. exactly the `DateTimeOriginal` the export stamps on that frame (see Export).
- **Thumbnail pipeline** (`convert_thumbnail(image, max_size, tone, crop, rotation)`): `rawloader::decode_file` → `downsample_thumbnail` (fused normalize + crop + **phase-preserving block average straight from the sensor buffer**, never materializing a full-res buffer: bayer classes rescaled onto one common base per `class_gains`, cpp≥3 via luma-of-channel-means which is exact because luma is linear) → `unsharp_mask` (true sensor space, `UNSHARP_AMOUNT`=0.4, [1,2,1]² blur) → the one shared tone tail `bake_tone` (see the Tone model bullet; for an inverted/film preset the `2^-EV` gain hits the true sensor data first, then `film::invert_gray`, with the pivots derived EV-exact from the raw sensor fractiles just like the detail shader; an already-positive scan gets histogram `tone_anchors` + curve, then the `2^EV` gain) → sRGB → u8 quantize as `[v,v,v,255]` → crop/rotation geometry (`crop_rgba` then `rotate_quarters`, `orient_mono`-mirrored) → `orient` → `Handle::from_rgba`. Accepted tone swap: per-class bases and the invert anchor are measured at thumbnail scale after the downscale instead of on full-res samples (flatten-before-average), so high-contrast blocks can differ subtly from the detail render. Thumbs decode progressively, up to `MAX_CONCURRENT_THUMBS` (4) in flight at once on the blocking thread pool, in file order. Roll covers decode through the same pipeline **baking in the cover file's stored exposure/tone/crop/rotation from the roll's manifest** — so a roll tile preview mirrors the edited frame (untouched rolls stay byte-identical).
- **Film stock & base measurement** (`src/film.rs`): `MonoStock { name, base, d_max, gamma }`; `ACTIVE_STOCK` = Ilford HP5+ (`base=0.82`, `d_max=2.4`, `gamma=0.7` — pending visual tuning). Inversion (shared `positive()` helper): density `log10(base/v)` → normalized position `clamp(density/d_max, 0..1)` → tone curve `position^gamma`. Clear film prints black, the d_max limit prints white. `MIN_PLAUSIBLE_BASE` (0.1) guards against images with no measurable margins. For an already-positive scan (preset `None`) the linear data is just normalized via `normalize_positive` (p95-bright percentile anchor, clamp [0,1]) with no density inversion.
- **Detail view**: double-clicking a tile's image overlays a detail view for that frame on an opaque surface that hides the still-mounted thumbnail grid (the grid never leaves the widget tree, so iced preserves its scroll position across open/close). The detail surface is painted with the window background color (`theme.cosmic().background(false).base`) so the letterbox bars show the same backdrop — in crop mode it switches to white; a full-surface `MouseArea` (`Message::Ignore`) swallows pointer input over the caption/bars while the preview itself is the interactive `DetailArea`. While the GPU decode lands, the cached 384 px thumbnail is shown; once the shader is ready, the thumbnail fades OUT 1→0 over `FADE_SPEED = 5.0` (driven by a `time::every` animation subscription) so the GPU mono texture takes over without flicker (the fade was flipped from fade-IN because the GPU layer is bottom-to-top in the `Stack`). Escape closes the view and drops all detail buffers (the overview LRU survives). Only ONE critical hi-res decode is in flight; selecting another file queues behind it (stale results discarded by name). A separate tiny channel preloads neighbor overviews into the LRU so Left/Right paging is instant. Loading is progressive through that single slot: a 2048-edge overview first, and a native-cap re-decode once the zoom reaches the **level-up trigger** — the earlier of the current texture's 1:1 ("100%") cap and `NATIVE_ZOOM_THRESHOLD` (2× contain) — that replaces the texture in place (same framing, no crossfade). The cap term matters: the wheel clamps zoom at 1:1, so on a wide full-screen preview (context drawer closed) the overview's 1:1 can sit below 2.0 and a *fixed* threshold could never be crossed — the level-up would never fire (the hi-res "does not load when drawer closed + full-screen" bug); firing at the cap is exactly when the overview is at 100% and deeper zooming needs native. While that native re-decode runs the wheel keeps working: the zoom clamp rises from the overview's 1:1 to the **projected native 1:1** (`max_detail_zoom_for_wheel` — the source's own 1:1 computed via `shader.zoom_100_for` for the texture `resized_dims(src, MAX_TEXTURE_EDGE)` will produce), so you can keep zooming through the load but never past the pixels native pixels cover, and the landing re-clamp re-derives that same value exactly (no yank-back); `reclamp_detail_zoom` uses the projected cap too, so a mid-load window/drawer resize or crop edit can't snatch the zoom back to the overview's 100%. A cached overview shows the shader immediately (no thumbnail fade). A header-top-right spinner (`indeterminate_circular`, 18 px, beside the export ring) runs while any hi-res decode is in flight for the open detail view; it is gated on `selected.is_some() && detail_inflight.is_some()` because `detail_inflight` is NOT cleared when Escape closes the view (only roll-close clears it) — the `selected` term keeps a still-running decode from spinning on the grid below.
- **Zoom & pan**: the preview surface is wrapped in `DetailArea`; the wheel zooms (one notch ≈ half a `log2` unit, gated on the cursor being over the preview itself) and press+drag pans. The maximum zoom is the **1:1 ("100%") cap** — one image pixel per one physical screen pixel — computed dynamically as `1 + log2(max(dw/sc_w, dh/sc_h))` from the loaded texture (cropped + rotation-swapped) and the preview area's physical size (`DetailProgram::zoom_100`), clamped to `[1.0, max_detail_zoom()]` behind the `MAX_DETAIL_ZOOM = 7.0` absolute ceiling; the cap is re-derived + re-clamped whenever it can change (preview resize, drawer open/close, a native level-up installing a larger texture, crop/rotation edits, crop-mode padding). Zoom is **continuous from the contain fit**: `scale = contain_s · 2^(zoom−1)`, so at `zoom == 1` the view is byte-identical to `ContentFit::Contain` and bars recede gradually; the frame fills the shader at `1 + log2(cover/contain)` and keeps overflowing beyond (never clipped to the letterbox band — explicit user requirement). The pan keeps the image point under the cursor pinned across zoom steps (`zoom_about_anchor`); `DetailArea` supplies the cursor **center-relative** (iced's plain `MouseArea` gives top-left-relative positions, which would shift the anchor by the unknown widget center). Zooming back out to 1 re-centers (fit view must be centered); zooming out past cover does not snap. Pan is unclamped (bars show when dragging past an edge). `on_release` also fires on cursor-leave-while-pressed and on `CursorLeft`, so a grab-pan can never stick; wheel zoom is additionally guarded to the preview so scrolling the drawer/bars never zooms.
- **Tone model (persisted, orthogonal controls — exposure, contrast, highlight rolloff, shadows)**: **Exposure** is a `2^EV` gain (everything moves together) plus three **pivoted powers** — **Contrast** `kc` (pivot at the image's measured **mid-gray** `m`: `C(p)=m^(1−kc)·p^kc`), **Highlight rolloff** `kr` (pivot at the measured **white point** `w` ≈ 98th %ile: `R(p)=w^(1−kr)·p^kr`), and **Shadows** `ks` (pivot at the measured **shadow anchor** `s` ≈ 10th %ile: `S(p)=s^(1−ks)·p^ks`). The pivot-at-an-image-anchor framing is the whole point: the previous panel's D-max/gamma were *mathematically* near-duplicates of exposure (at default gamma, D-max `T(p)=(2.4/d)^0.7·p` is literally a pure gain ≈ ±0.9 EV). Three monotone powers still compose **exactly** into one power `ratio·p^exp` (`ratio = s^(1−ks)·w^(ks·(1−kr))·m^(ks·kr·(1−kc))`, `exp = kc·kr·ks`, `curve_remap` in `shader.rs`); the GPU consumes it through the 2048-entry tone LUT (`build_tone_lut`), the CPU bakes call `shader::tone_model` directly — at defaults `kc=kr=ks=1` the remap is the identity and untouched renders stay (sub-1e-6) identical. Anchors are measured once per detail decode: an already-positive preset gets `tone_anchors` (single-pass 4096-bin histogram, O(n), 10th/50th/98th %iles clamped to ≥ `MIN_ANCHOR`=1e-3 so `pow(0, negative)` can't NaN degenerate all-black frames); an inverted (film) preset gets the raw sensor fractiles (`film_anchor_fractiles`, stride `ANCHOR_INV_STRIDE = 4`) measured once, with the pivots derived at the CURRENT EV (`film_pivots_at_gain` — `set_exposure` re-derives on every slider tick, so the anchors always describe the actual render, EV-exact, closing the old detail-vs-bake anchor drift). `curve_remap` also floors the shadow pivot at `mid · SHADOW_PIVOT_FLOOR` (0.15) so a hard-black frame keeps a usable Shadows power. Caveats accepted: the LUT re-rounds through the f16 texture and linear interpolation (gamma-domain packed toward black, ≤ ~1e-3 pre-exposure, sub-visual); unsharp edge detail was computed pre-curve and is re-curved rather than re-derived. The only approximation anywhere is the LUT itself, bounded by `tone_lut_reproduces_tone_model_within_tolerance` (≤1e-3) and `cpu_bake_matches_gpu_lut_simulation_on_random_frames` (≤5e-4 over randomized frames/EV/curve/preset/base-mode, simulating the GPU via `sample_tone_lut_f32` + `half_to_f32`), replacing the manual detail-vs-export eye-check; the LUT is rebuilt into the shader by `rebuild_tone_lut` (keyed by `tone_version`) on every curve/EV change. Grid == export == detail hold structurally because this single body (plus those parity tests) is the only place the tone math lives for the non-shader paths.
- **GPU exposure**: the detail view runs through an `iced::widget::shader::Shader` that uploads the linear mono `Vec<f32>` once as an `R16Float` texture at decode time (single channel, f32→half in `Primitive::prepare` via `mono_to_half_bytes`; **half-float is essential — `R32Float` is not filterable on WebGPU/wgpu 28** and would fail the bind-group validation). The WGSL shader applies `2^EV` in linear light and sRGB-encodes per fragment. `ExposureChanged(f32)` writes the new EV into a uniform via `shader.set_exposure(ev)` — zero CPU work, zero per-frame `Handle` allocation, no debounce. The `Uniforms.exposure` field carries the sensor-space gain, NOT the raw EV: `2^EV` for an already-positive preset, `2^-EV` for an inverted (film) preset (`sensor_gain(ev, inverted)` — the sign flip happens at the point EV meets the sensor data, so +EV always brightens the positive); at EV=0 the gain is exactly 1.0. The texture is uploaded with a **full CPU-built mip chain** (`build_mip_chain`: linear-light 2×2 area-average halving of the f32 mono, `mono_to_half_bytes` per level, uploaded level-by-level in `prepare` with `mip_level_count = chain len`) and the sampler sets `mipmap_filter: Linear` — so minified views (e.g. zooming back out to contain after the native level-up installs an up-to-8192 texture) sample pre-filtered texels instead of aliasing the full-res film grain into a moiré. Mips average the TRUE sensor data before the WGSL's per-fragment tone/inversion, matching the CPU bake's flatten-before-average ordering; magnification keeps LOD 0, so zoomed-in renders stay byte-identical.
- **Editing panel**: with a detail view active, the editing panel is the COSMIC **context drawer** side pane (`core.window.context_is_overlay = false`, so the image shrinks to make room). It follows the detail view's **per-view remembered state** (`drawer_memory`); the user raises it with **Space** or **View → Details…** — there is no header toggle anymore. Content, top to bottom: title; the exposure slider (−3.00 to +4.00 EV, step 0.01, base +0.7 default); the **persisted** tone controls — Contrast, Highlight rolloff, Shadows sliders (no numeric readouts): **Contrast** is the raw power (`0.25..=4.00`, 1.0 identity, up = more contrast); **Highlights/Shadows** present their stored power through a **stop-based lift value** (`app::curve_lift_value = -log2(power)`, symmetric track `-2.0..=2.0` stops, identity at the centered `0.0`) so dragging RIGHT **brightens/lifts** the region by the same perceptual amount either way from center — the direction other photo apps use, with the useful band spread across the track instead of crammed near an edge — while the stored `curve_rolloff`/`curve_shadows` power, the GPU LUT math, and every manifest/bake keep their original meaning (the flip exists only at this UI/keyboard boundary); a divider; a `Rotation: N°` line; the conditional `Crop: T R B L` readout (shown only while a crop exists); then the reset row (**Reset all edits** restores every edit to the state the panel was opened with). While the GPU shader is loading the panel shows only the title. The drawer width comes from libcosmic's `context_width()` (344–480 px).
- **Crop (movable/resizable window, keyboard-driven, aspect-locked)**: the crop is a sub-rectangle of the source, **moved** and **resized** in **crop mode** only (a VIM-like modal state: bare `c` or **View → Crop mode** toggles it; while on, every non-crop edit key is inert, the image is shown over **white** with everything outside the crop **dimmed** by the shader's `overlay_sample` layer, and Esc exits crop mode first — one more press closes the view). Keys: `h`/`j`/`k`/`l` **and** the arrows move the window (`CROP_STEP_PX`=2, Shift=`CROP_NUDGE_PX`=1), `-`/`=` resize it about its center (`CROP_RESIZE_STEP_PX`=4, Shift 1 px) — move keeps size/aspect, resize scales by `s = 1 + delta/long_edge` so the aspect is preserved exactly, both clamped to `[MIN_CROP_WINDOW=4, full frame]`. Margins are stored in **source pixels** per edge as `CropMargins { top, right, bottom, left }` on `EditData` — a window is just `left=x, top=y, right=W−x−w, bottom=H−y−h`, authored in the **display-oriented source frame** (post-masked-borders, EXIF-rotated upright; keyboard trim bounds resolve the true source dims via `DetailProgram::source_dimensions()`, restored from the downscaled texture + `src_long_edge` in `DetailProgram::new`, NOT the 2048-overview dims), and interpreted *fractionally* at any texture resolution (the shader's `crop_uv_geometry` maps by `tex/src` per axis, and `display_source_dims`/`scale_crop` do the same for bakes — the old overview-dims reference bug that moved the stored crop's meaning between levels is fixed). Film-frame moves are **visual/rotation-aware**: `crop_move_direction(MoveDir, rotation)` cycles the direction by `rotation & 3` CCW turns, matching the shader's `rotate_uv`. Live preview is uniform-only zoom-to-fit: `view_uv` anchors the contain/zoom scale to the CROPPED frame, so trimming re-fits the kept content. **Baked everywhere** in grid/cover/export as `crop_rgba` then `rotate_quarters` (the detail shader path does NOT re-crop — it would double-crop). Commit cadence: moves are live and persist on crop-mode close / flush points (`commit_edit()`); **Reset crop** zeroes only the crop, then persists immediately.
- **Display rotation (per-frame, persisted + baked)**: `r` rotates the detail view counter-clockwise in 90° quarter-turns, composed AFTER the crop and on top of the EXIF orientation (`rotate_ccw(crop(orient(EXIF)))`; crop margins stay authored in the EXIF-upright frame). `AppModel.rotation: u8` (0–3, `& 3` clamped) is loaded per open from the manifest, applied to the shader as a **uniform-only** `rot: f32` (`rot == 0` renders byte-identical); WGSL helpers `rotate_uv`/`rotated_dims`/`crop_box_display` drive `view_uv` + the dim overlay, pinned by Rust mirror tests (`rot_uv`/`rotated_dimensions_...`). Persisted as `EditData.rotation: u8` (manifest v1); **not** part of `ToneEdit`, so copy/paste excludes it (like crop) and `set_tone` preserves it. Baked into grid tiles and roll covers via `rotate_quarters(rgba, w, h, turns)` after `crop_rgba`, so tiles match the rotated detail view.
- **Selection & clipboard**: frame selection is `selected_frames: HashSet<String>` (drives the selection ring + paste target) alongside the primary `frame_selected` focus. `apply_frame_click` is pure: plain = clear+select, Ctrl+click toggles, Shift+click selects the range from `selection_anchor`. Ctrl+A selects the whole roll; Shift+arrow extends from the range, plain arrow collapses. Modifier state is read from the single `AppModel.modifiers` field (see Keyboard) rather than tracked per key. **Copy/paste edits**: Ctrl+C copies the focused frame's full `ToneEdit` into `clipboard: Option<ToneEdit>`; Ctrl+V applies it to every selected frame (falling back to the focused frame), writes each via `RollManifest::set_tone`, syncs the live shader + panel when the open frame is a target, persists, and re-bakes the affected grid tiles (plus the cover if targeted). Because crop/rotation live on `EditData`, not `ToneEdit`, they are inherently excluded from paste.
- **Edit persistence**: each image's exposure and tone-curve power edits (plus crop + rotation) survive image switches, detail close, and app restarts via a per-roll manifest `.film-roll.toml` (TOML, `{ version, name?, base_mode?, calibration_frame?, base?, start_date?, end_date?, preset?, edits: { <name>: { exposure_ev, curve_contrast, curve_rolloff, curve_shadows, crop: { top, right, bottom, left }, rotation } } }`), schema **`version = 1`**. The manifest file name is deliberately function-based rather than app-based so an **app rename never orphans existing sidecars** (remember also `APP_ID = "io.github.jpttrssn.curvectrl"` and `CARGO_PKG_REPOSITORY` when renaming; `Config` follows `APP_ID` automatically). `RollOpened` loads the active roll's manifest once on drill-in and `reconcile()` drops entries whose file left the scan, so a recycled file name can never reattach stale edits. Writing is RAM-first: `ExposureChanged`/`CurveChanged` mutate `roll.edits` only; flushes happen on slider drag-end (`EditSave`), on `DetailClosed`, and on window close; `save_roll_manifest` writes a temp file then renames (atomic). Reads consolidate on `ToneEdit` (a `pub` aggregate of exposure + the three powers, identity `Default`) via `RollManifest::tone(name)`. `decode_next()` reads the full `roll.tone(name)` (+ crop/rotation) at spawn and `convert_thumbnail` bakes it all in, so grid == detail; `EditSave` additionally re-bakes the selected tile and, in lockstep, the roll cover when the edited frame is the cover file. **Reset all edits** (`ResetAll`) restores the snapshot captured at open (`reset_exposure_ev`/`reset_curve_*`/`reset_crop`/`reset_rotation`), not the identity — idempotent and anchored. Storage is parameterized by directory, so multi-roll is structural; the known trade-off is that renaming/moving a file *inside* a roll orphans its edit (the name is the key); TOML keeps the file hand-editable.
- **Menus**: `header_start` renders File (Add roll, Remove roll — enabled only on the rolls view with a roll selected — divider, Export…, Quit), Edit (Select all, Copy edits — enabled while a frame context exists, Paste edits — additionally requires a non-empty clipboard), and View (About, Details…, Crop mode). Items map through `MenuAction` → `Message`; the menu bar uses `window_id_maybe(main_window_id)` + `on_surface_action(Message::Surface)` (cosmic-files' wiring, so the popups are real Wayland surfaces rather than the in-window overlay, which per the pinned libcosmic would not paint until the next mouse move) and `item_width(menu::ItemWidth::Uniform(360))` matching cosmic-files. **View → Details…** is the single generic drawer item (gated on a detail view being open or a roll selected on the rolls view; its key_bind hint shows `Space`). Quit persists the RAM roll + config then returns `cosmic::Action::Cosmic(cosmic::app::Action::Close)` so the window close hook runs a final flush.
- **Keyboard & shortcuts**: Escape and Ctrl+F route through libcosmic's built-in `keyboard_nav` (`on_escape()`/`on_search()`); a `keyboard::listen` subscription handles the rest, all gated on `Status::Ignored` (a focused text input captures first). Bare keys: `h`/`j`/`k`/`l` mirror the arrows (`Nav`) everywhere — library selection, frame-grid highlight, detail paging (Left/Right), and crop-window moves in crop mode; Enter opens the selected cell/frame; **Space** toggles the current view's context drawer (the editing panel / frame-info / roll-info, each *per-view remembered* via `drawer_memory` — navigation never closes a drawer, the drawer X closes and clears its slot, stale targets clear it; text inputs swallow bare Space while typing); `?` toggles the global **help overlay** listing every shortcut in four sections (General/Navigation/Editing/Crop mode) above the toaster; selection-less presses land on the first item (`nav_target`, pure + unit-tested; nav indexes the unified `library_cells` set via `library_cell_index`, so the Add Roll tile is reachable cell 0); **scroll-into-view**: whichever grid is mounted reports its geometry via `.on_scroll(Message::GridViewport)` into the cached `grid_viewport` (cleared on roll open/back), and `Nav` reveals a selection that steps beyond the visible area — `nav_cols()` derives the column count from the viewport width (iced's `ceil((avail+spacing)/(max+spacing))`), the pure `reveal_target_y` computes the content-space target (`top < offset` → scroll up to `top`; `bottom > offset + viewport_h` → scroll down to `bottom − viewport_h`, clamped to `[0, content_h − viewport_h]`), and the handler returns `scrollable::scroll_to::<cosmic::Action<Message>>(id, AbsoluteOffset { x: None, y })`; grid ids are `rolls-grid`/`frames-grid` (`scrollable_id`, only one page mounted at a time). Before the first real scroll the viewport is estimated (offset 0, `window_height − 80` from `on_window_resize`, content from the row count); the programmatic scroll itself refires `on_scroll` and takes over precisely. `c` toggles crop mode; `e` exports; `r` rotates; `-`/`=`/`[`/`]`/`;`/`'`/`,`/`.` step Exposure/Contrast/Rolloff/Shadows (bare step, Shift = fine nudge) — inert without a detail view, and a held editing key commits **once on release** (`editing_key_held`, mirroring the slider's drag/release cadence — no per-auto-repeat decode/TOML write). Modifiers are read from the single `AppModel.modifiers: keyboard::Modifiers` field updated by `ModifiersChanged` (this iced fork derives `KeyPressed.key` from winit's `key_without_modifiers`, so shifted symbols never arrive — modifiers are read from the event, never guessed). Escape cascade: help → crop-mode exit → close the open drawer → close the detail view → back out of the roll (one press from the bare grid; the frame highlight is dropped as part of the reset) → deactivate the library search. The subscription uses `Subscription::filter_map` with a **non-capturing** (zero-sized) closure, so mode flags (crop/help) are read in the message handlers, not the closure.
- **Export**: **File → Export…** / bare `e` exports the current selection (every multi-selected frame, else the focused frame; disabled/no-op without a target) into a folder chosen in the **native open-folder dialog** (`cosmic::dialog::file_chooser::open::Dialog::open_folder()`; the portal choice list has a **format dropdown** — `jpeg-90` (JPEG 90% full-res, 300 dpi), `jpeg-82` (JPEG 82% at 2048, 72 dpi), `png-16` (16-bit PNG full-res, 300 dpi), resolved by the pure `options_for_choice` — and an **overwrite checkbox**, default off). `begin_export` snapshots edits from the **in-memory** roll (so exports always match the screen, even for an un-flushed edit), then `export_frames` runs sequentially on the blocking pool; overwrite-off skips any existing `<name>.<ext>` **before decoding**. **Naming**: undated rolls keep `<stem>.<ext>`; dated rolls export as `YYYYMMDD-<roll-hash>-<frame>.<ext>` — start date + a deterministic FNV-1a 6-hex `roll_hash(dir)` (masked to 24 bits, deliberately NOT std's `DefaultHasher`, which is unstable across Rust versions) + 1-based zero-padded full-roll position, produced by the pure `dated_export_name(format, start_iso, index, roll_hash)` (returns `None` for a malformed start date, falling back to the plain name) — so a plain filename sort groups a same-day multi-roll folder by roll. **EXIF stamping** (dated rolls): `DateTimeOriginal` = start date at 00:00:00 + the frame's full-roll position in seconds (synthetic film-capture time), `DateTimeDigitized` = the RAW's own scan time when present (read as its raw EXIF text via `exif_writer::raw_datetime_original` — NOT `load_frame_meta`'s display value, which kamadak normalizes to hyphen form; the writer enforces the exact `YYYY:MM:DD HH:MM:SS` colon shape), via the hand-rolled TIFF blob spliced as JPEG APP1 (`jpeg_app1`/`splice_after_soi`) or PNG `eXIf`. Encoders are luminance-only (negatives are monochrome today): Luma JPEG (`jpeg-encoder`) and 16-bit grayscale PNG via the `png` crate directly (big-endian samples, `set_pixel_dims` adds `pHYs`), with density tags (JFIF dpi / `pHYs` px-per-meter) and a `sRGB` chunk on the PNG; geometry is `crop_rgba` then `rotate_quarters` (`bake_geometry`/`bake_geometry16`). Every file is written atomically: encode to `<name>.<ext>.tmp` then rename over the target. A small 18 px **determinate circular ring** in `header_end` shows progress while a batch runs (`ExportProgress { done, total }` streamed over a channel; zero space when idle); File→Export/`e` refuse to stack a second batch; completion toasts report done / skipped (overwrite-off) / failed, with the `(dated {date})` variant for dated rolls. The roll scan skips `.jpg`/`.jpeg`/`.png` files (`is_export_artifact`), so exporting into a roll's own folder never shows the outputs back up as fake frames or the cover. When color negatives eventually land, the encoders must restore the RGB/RGBA interleave (see the note in `export` — `png::ColorType::Rgb16`, `ColorType::Rgb` for JPEG).

### Architecture (model in `src/app.rs`, GPU in `src/shader.rs`)

- State: `tiles: Vec<Tile>`; `Tile { name: String, thumb: Thumb, meta/meta_failed }`; `Thumb::{Loading, Ready(Handle), Failed}`. Detail state: `selected: Option<String>`, `detail_shader: Option<DetailProgram>` (set after one decode, dropped on close/reselect), `detail_inflight: Option<String>`, `detail_native_queued: bool` (one-shot level-up gate), `detail_thumb_opacity: f32` + `detail_last_frame: Option<Instant>` (fade), `detail_thumb: Option<Handle>`, `exposure_ev: f32`, `detail_zoom: f32` (log2 units ≥ 1), `detail_pan: (f32, f32)` (widget-center-relative physical px), `detail_panning: bool`, `detail_cursor: Option<Point>` (kept while hovering so wheel zoom anchors correctly), `stock: MonoStock` (non-persisted preview stock; reset to `ACTIVE_STOCK` in `clear_detail()`), `roll: RollManifest` (RAM copy, loaded in `RollOpened`, reconciled against the scan), the reset snapshots `reset_exposure_ev`/`reset_curve_contrast`/`reset_curve_rolloff`/`reset_curve_shadows`/`reset_crop`/`reset_rotation` (what `ResetAll` restores), `crop: CropMargins` + `rotation: u8` (live, pushed to the shader via `set_crop`/`set_rotation`), `rolls: Vec<Roll>` (library cards — `Roll { dir, name, cover, frame_count, thumb, preset }`), `active: Option<PathBuf>` (drilled-in roll dir; `None` = library page), `library_selection: Option<LibrarySelection>` (`AddRoll` or `Roll(PathBuf)`; drives highlight, drawer target, Enter/arrow nav; set through `set_library_selection` so the roll-name/date drafts re-seed in the same pass), `frame_selected: Option<String>` (frame-grid highlight) + `selected_frames: HashSet<String>` + `selection_anchor` (multi-select), `clipboard: Option<ToneEdit>`, `modifiers: keyboard::Modifiers`, `drawer_memory: DrawerMemory` (three bools keyed by `DrawerView { Library, Grid, Detail }`), `crop_mode: bool`, `help_visible: bool`, `grid_viewport: Option<Viewport>` (drives precise keyboard scroll-into-view), `window_height: f32`, `grid_cols: usize` (seeded 3 until first resize), `search: Option<String>` (live typed text + active/toggle gate) + `search_filter: Option<String>` (applied term) + `search_version: u64` (debounce serial), `thumb_inflight: Vec<String>` / `cover_inflight: Vec<PathBuf>` (the ≤`MAX_CONCURRENT_THUMBS` decode caps), `frame_meta_inflight`, `roll_date_drafts: RollDateDrafts` (keyed by the selected roll) + `roll_name_draft: String`, `export_progress: Option<(usize, usize)>` + `export_pending: bool`, `detail_cache: LruCache<(PathBuf, String), DetailMono>` (LRU of detail **overviews** by (roll dir, file name), cap `DETAIL_CACHE_CAPACITY` = 40, survives roll switches/detail close; native level-ups never cached; `retain` evicts a roll's entries on preset change), `detail_preload_inflight: Vec<(PathBuf, String)>`. Helpers: `clear_detail()` resets all detail buffers incl. zoom/pan/tone-preview/crop-mode state (the LRU survives by design); `handle_detail_ready()` applies a completed decode and re-applies zoom/pan + tone curve; `install_detail_shader()` builds a shader from a cached/decoded mono (cache-hit path); `preload_detail_neighbors()`/`handle_detail_preloaded()` fill the LRU with the `DETAIL_PRELOAD_DISTANCE` neighbor frames; `commit_edit()` = `persist_roll()` + `re_bake_edit()` (the single commit point); `persist_roll()` flushes `roll` to disk.
- Messages: `RollsLoaded(Vec<Roll>)` fills the library, pre-selects the first visible roll, starts the cover chain; `RollInfoLoaded(Roll)`/`CoverReady(PathBuf, Result<Handle, ()>)` feed card covers (dedup by dir, name-sorted; `RollInfoLoaded` also continues the cover chain without re-selecting); `AddRoll`/`RollAdded(PathBuf)` open the folder picker and drill straight into the new roll's grid (anywhere in the app; the selection survives the roll-exit reset); `RollSelected(PathBuf)`/`AddRollSelected` set the library selection (through `set_library_selection`); `OpenSelected` (Enter)/`RollActivated(PathBuf)` (double-click) open the selected cell through `open_roll`; `Nav(MoveDir)` arrow/VIM movement, page-aware (library cells over `library_cells`, frame highlight, detail paging, or crop-window move in crop mode); `FrameSelected(String)` routes through `apply_frame_click` (plain/Ctrl-toggle/Shift-range from `selection_anchor`); `SelectAllFrames` (Ctrl+A) fills `selected_frames`; `CopyEdits` (Ctrl+C)/`PasteEdits` (Ctrl+V) round-trip `clipboard: Option<ToneEdit>` through `set_tone`; `RollOpened(PathBuf, Vec<String>)` builds tiles, pre-selects the first frame, starts the thumbnail chain (stale-scan guarded against `active`); `SearchActivate`/`SearchClear`/`SearchInput` manage the header input, `SearchCommitted(term, serial)` applies the debounced filter (only when `serial == search_version`); `ThumbReady(String, Result<Handle, ()>)` stores + chains; `ThumbnailActivated(String)` opens a frame (`open_frame`: persists outgoing edits, loads stored tone/crop/rotation, sets `selected` + `frame_selected`, pumps detail); `DetailReady`/`DetailPreloaded`/`DetailFadeTick` drive the detail pump + crossfade (stale results filtered by `detail_result_is_current`); `ExposureChanged(f32)`/`CurveChanged(f32, f32, f32)` write the shader uniform + RAM edit live; `EditSave` flushes (slider release, `DetailClosed`, window close); `DetailZoom`/`DetailPanPress`/`DetailPanMove`/`DetailPanRelease` drive the pure zoom/pan helpers; `ResetAll` restores the open-time snapshot; `ToggleContext` (bare Space) toggles the current view's drawer via `ToggleContextPage` (so the page guards still apply); `AdjustEdit(EditAdjust)` steps the edit keys (via the pure `edit_adjust_for(key, shift)`), `EditKeyReleased` commits a held key once; `ModifiersChanged(Modifiers)` maintains the single modifier source; `DetailClosed` (Escape) closes the drawer, then the detail, then backs out of the roll (resetting every roll-page field; the overview LRU is kept); `RemoveSelectedRoll`/`RemoveRoll` remove a roll non-destructively; `RollPresetChanged(dir, preset)`/`RollDateDraftChange`/`RollDateDraftSubmit`/`RollNameDraftChange`/`RollNameDraftSubmit`/`CalibrateFromFrame` edit roll metadata (commit, persist manifest, re-bake tiles/cover on preset change); `FrameInfoReady` lands lazily-parsed EXIF; `ExportRequested`/`ExportChosen`/`ExportProgress`/`ExportDone` drive the export pipeline (portal dialog → sequential bake → streamed progress + toast); `ToggleCropMode`/`ToggleHelp` toggle the VIM crop mode and the `?` overlay; `Ignore` (no-op) consumes events aimed at the detail/help blockers.
- Decode chain: `decode_next()` fills up to `MAX_CONCURRENT_THUMBS` slots with distinct `Loading` tiles → one `tokio::task::spawn_blocking` per file (`decode_thumbnail`, reading tone/crop/rotation from the active roll at spawn), returned as a `Task::batch`. `thumb_inflight` enforces the cap and de-duplicates: a name already running is never spawned again (a mid-chain re-bake waits instead of racing), and the EV + tone curve are read at spawn time — startup tiles bake stored edits, re-baked tiles the freshest. `ThumbReady` removes the finished name and pumps; `RollOpened` clears the set, the final `DetailClosed` roll-exit drops it, `RollsLoaded` clears it alongside a new round. Roll covers are pumped by the sibling `decode_covers()` — same cap/discipline, keyed by dir.
- Detail pump: `decode_detail_next()` spawns only when no decode is in flight and something is selected. **Progressive two-level loading through the single slot**: with no shader yet it decodes the 2048 overview; once `detail_zoom` reaches the **adaptive trigger** `native_level_up_zoom()` — the earlier of the current texture's 1:1 cap (`min(max_detail_zoom(), NATIVE_ZOOM_THRESHOLD)`; see the level-up-reachability bug fix in the Detail view bullet) — a wheel fires a native-cap re-decode (`MAX_TEXTURE_EDGE` = 8192) whose arrival swaps the texture in place — `fresh_open` (shader absent) gates the thumbnail fade so a level-up never re-flashes the thumb, and the swap re-applies zoom/pan + tone curve. `detail_native_queued` makes the level-up one-shot, and it's set early when the sensor's true long edge (`src_long_edge`, threaded out of `decode_raw_detail`) never exceeded `HI_RES_SIZE` — that *source* edge, NOT the capped overview width, decides native-ness (a large sensor downscaled to just under 2048 was the original false-positive). A superseded decode's arrival frees the slot via the stale guard and re-pumps; the wheel trigger is additionally gated on `detail_inflight.is_none()`, so wheels that keep zooming through a running decode (up to the projected native cap) never re-enter the busy slot — the landing re-check (`handle_detail_ready` re-checks the trigger after freeing the slot, `Ok` landings only) covers a zoom taken mid-load. Failures do NOT auto-retry (re-select retries; after an `Err` the zoom trigger stays live so the next wheel retries). When an overview is requested, `decode_detail_next` first consults `detail_cache` — a hit builds the shader from cached mono with no decode/fade; misses decode and insert on landing. `open_frame` also calls `preload_detail_neighbors` (fills the LRU `DETAIL_PRELOAD_DISTANCE` frames either side, honoring `MAX_CONCURRENT_PRELOADS`, skipping cached/in-flight). Edits are applied in-shader at open time, so a cached mono stays valid across edit changes.
- Thumbnail pipeline (`convert_thumbnail(image, max_size, tone, crop, rotation)`): `rawloader::decode_file` → `downsample_thumbnail` (fused normalize + crop + phase-preserving block average straight from the sensor buffer, never a full-res buffer) → `unsharp_mask` (sensor space) → the one shared tone tail `bake_tone` (see Decisions; `2^-EV` + `film::invert_gray` for a film preset, `tone_anchors` + `2^EV` for a positive) → sRGB → u8 → crop/rotation geometry (`scale_crop`→`crop_rgba`→`rotate_quarters`) → `orient` → `Handle::from_rgba`. Grid == export == detail hold structurally because this single body (plus its GPU-LUT parity tests) is the only place the tone math lives for the non-shader paths.
- Detail pipeline (`decode_raw_detail`): same as the thumbnail pipeline through `unsharp_mask` on `Vec<f32>` mono, then `orient_mono`, returning `DetailDecode { mono, width, height, src_long_edge, inversion }`. The mono is TRUE sensor-linear for every preset (no CPU inversion); `inversion: Option<(MonoStock, f32)>` is `Some` for a film negative (the EV-0 clear-film anchor measured per frame) or `None` for a positive. `src_long_edge` (post-crop, pre-downscale) decides whether the overview was already native. The downscale cap is a parameter: `HI_RES_SIZE` at overview level, `MAX_TEXTURE_EDGE` at level-up; `resize_area` treats a cap ≥ both source edges as identity, so ≤8K/small sensors decode at full resolution. The GPU shader does the per-fragment density inversion (film) + sRGB encoding.
- **GPU pipeline** (`Pipeline::new` in `src/shader.rs`): one-time `ShaderModule` from `include_str!("shader/exposure.wgsl")`, `BindGroupLayout` (texture, sampler, uniform buffer), `PipelineLayout`, `RenderPipeline` (fullscreen triangle, target `Bgra8Unorm` to match iced's framebuffer), `Buffer` for `Uniforms`, `Sampler` (linear + ClampToEdge). Texture + bind group are created lazily in `Primitive::prepare` on the first frame (they need the mono data); later frames only update the uniform buffer via `queue.write_buffer`. The WGSL `fs_main` already applies `linear_to_srgb` per fragment, so the target is `Unorm` (linear) — `UnormSrgb` would double-encode; if libcosmic ever switches to an sRGB-aware framebuffer, drop/no-op `linear_to_srgb` and swap back to `Bgra8UnormSrgb`. Texture re-upload is keyed by `image_id: u64` (`AppModel.next_image_id`, `wrapping_add(1)`) remembered in `DetailPipeline::current_image_id` — without it iced reuses the same pipeline across selections and the old mono stays bound ("second image still shows first").
- WGSL uses the canonical 3-vertex fullscreen triangle (`-1,-1 / -1,3 / 3,-1`) with NDC passed through directly. The earlier `(vec2(1u, 2u) + vi) % vec2(6u) < vec2(3u)` pattern produced only the upper-right half of the viewport, because under wgpu the render-pass scissor clips to the viewport rect rather than the original primitive bounds.
- Detail view layout: the shader widget is `Length::Fill × Length::Fill` directly inside a Fill × Fill `widget::container` — no wrapping Grid. Because `widget::Shader` has no `.content_fit()`, the WGSL does the `ContentFit::Contain` math itself. Letterbox/pillarbox bars are alpha-blended (`vec4(0,0,0,0)` + `BlendState::ALPHA_BLENDING`) so the COSMIC panel background shows through rather than solid black frames (the earlier `Grid::with_capacity(...)` approach failed because the Grid reports widget bounds `(available_width, available_width)` and `Primitive::render` uses `instance.bounds ∩ physical_bounds` for `set_viewport`, so the shader painted into the header). `detail_view()` is pushed onto a `Stack` OVER the always-mounted grid (blocking `MouseArea` in between; the preview `DetailArea` is nested inside so its `capture_event()` wins over the preview).
- **Per-frame uniform** (`Uniforms`, all f32): `exposure` (= the `sensor_gain`, `2^EV`/`2^-EV` per preset), `tex_w, tex_h` (texture dims for the contained math), the physical-pixel scissor rect `sc_x, sc_y, sc_w, sc_h`, `zoom` (log2, 1 = contain), `pan_x, pan_y` (image-center displacement, physical px — `prepare()` multiplies logical by `viewport.scale_factor()`), the crop box `crop_x, crop_y, crop_w, crop_h` (source-px margins collapsed to center/box for `crop_uv_geometry`), `rot` (quarter-turns), `show_mask` + `pad` (crop-mode dim overlay + the `CROP_MODE_PADDING = 100.0` margin that keeps a white surround even at exact aspect), and the film-inversion block `inv, inv_base, inv_d_max, inv_gamma`. The tone remap left the uniforms in the LUT rework: the 2048×1 `R16Float` `t_tone` texture (bindings 3/4) carries the curve, re-uploaded by `prepare()` only when `tone_version` changes. `view_uv` = `scale = min(sc_w/tex_w, sc_h/tex_h) · 2^(zoom−1)` about the centered crop, displaced by pan; fragments outside output alpha 0 (same blend trick as the bars).
- Constants: `THUMB_SIZE = 384.0` (decode cap + grid cell), `HI_RES_SIZE = 2048` (detail overview cap), `FADE_SPEED = 5.0` (thumb-fade-out/sec), `TILE_ASPECT = 1.0`, `UNSHARP_AMOUNT = 0.4`, `BASE_SAMPLE_TARGET = 250_000`, `MAX_CONCURRENT_THUMBS = 4` (a one-line knob, see the CPU-thumbnails decision), `DETAIL_CACHE_CAPACITY = 40` (LRU frames), `MAX_CONCURRENT_PRELOADS = 2`, `DETAIL_PRELOAD_DISTANCE = 1`, `MAX_DETAIL_ZOOM = 7.0` (≈64× contain ceiling on the dynamic 1:1 cap), `NATIVE_ZOOM_THRESHOLD = 2.0` (prefetch level-up trigger in log2, when the 1:1 cap sits above it; the actual trigger `native_level_up_zoom()` is `min(cap, this)` so it's reachable on every layout), `MAX_TEXTURE_EDGE = 8192` (native cap), `CROP_STEP_PX = 2`, `CROP_NUDGE_PX = 1`, `CROP_RESIZE_STEP_PX = 4`, `MIN_CROP_WINDOW = 4`, `MIN_LEFT: u32 = 2` (aspect-lock floor), `CROP_MODE_PADDING = 100.0`, `SEARCH_DEBOUNCE_MS = 300`, `MIN_ANCHOR = 1e-3`, `ANCHOR_INV_STRIDE = 4`, `SHADOW_PIVOT_FLOOR = 0.15` (· mid), `MIN_PLAUSIBLE_BASE = 0.1`.

### Environment gotchas

- `libcosmic` is a plain git dependency on pop-os master; Cargo.lock pins `ef490df5` (verified against the current lock). The local clone at `../cosmic/libcosmic` is no longer patched in. Upstream includes the `AspectRatio::layout` off-by-one fix and iced's `Grid` export. The commented `[patch]` template in `Cargo.toml` can restore the clone if needed.
- The `wgpu` feature on libcosmic is required for `iced::widget::shader::Shader`. Without it `DetailProgram` won't have a `Pipeline::new` entry point. `bytemuck` is the only binding-layer shim (Pod/Zeroable on `Uniforms`); `wgpu` itself comes through libcosmic's vendored re-export. `rayon` is only a transitive of `rawloader` now.
- Verify changes with `just check` (clippy pedantic) and `cargo test --locked`, always `--locked`. `just check` is **clean** for `src/`; the ~130 warnings from compiling libcosmic master from source are baseline — ignore them, only `src/` warnings matter.
- The i18n file is `i18n/en/curvectrl.ftl` and must match `CARGO_PKG_NAME`, else `build.rs` `Context::new` panics.
- Naming is fully agnostic: app `curvectrl`, module `shader`, GPU types `DetailProgram`/`DetailPrimitive`/`DetailPipeline`; fresh configs start with an empty roll list (the library then shows just the Add Roll tile).
- Detail-pump diagnostics: run with `EXPOSURE_TRACE_DETAIL=1` (default-off, `detail_trace`) to log the two-level decode lifecycle to stderr — trigger fire/swallow with the cap, arrival dims + `fresh_open`, decode errors, landing re-pumps.
- Re-bake diagnostics: run with `EXPOSURE_TRACE_REBAKE=1` (default-off, `rebake_trace`) to log the grid/cover re-bake lifecycle when a committed edit (crop or tone) should refresh the thumbnail — `re_bake_edit` firing, `decode_next` spawning (or why it no-ops), `ThumbReady` landing `Ready`/`Failed`/`not-found`. **Unified commit architecture**: all edit paths share `set_exposure`/`set_curve` (live RAM + shader) and one commit point `commit_edit()` (= `persist_roll()` + `re_bake_edit()`). Sliders stream `ExposureChanged`/`CurveChanged` (live) then commit once on release (`EditSave`); keyboard `AdjustEdit` steps are live-only and a held key commits once on release (`EditKeyReleased`); crop-mode close, `ResetCrop`, and `EditSave` also route through `commit_edit()`; `paste_edits` keeps its bespoke multi-target re-bake. Safe flush points: frame switch, `DetailClosed`, `on_close_requested`.

### Useful API facts (verified against pinned libcosmic/rawloader)

- `cosmic::Task<M>` aliases `iced::Task<cosmic::Action<M>>`; async work: `cosmic::task::future(async { Message::X(...) })`; batch with `Task::batch([...])`.
- `rawloader::{RawImage, RawImageData, Orientation}` are re-exported at crate root (`decoders` module is private).
- `RawImage` fields used here: `width`, `height`, `cpp`, `data`, `wb_coeffs` ([f32;4] RGBE), `blacklevels`/`whitelevels` ([u16;4]), `orientation`, `cfa`.
- `rawloader::CFA` is crate-exported; `CFA::new("RGGB")` builds test patterns; `color_at(row, col)` mods internally (`(row+48)%48`), so any coordinates are safe. Returns 0=R, 1=G, 2=B, 3=fourth-color. `shift(x, y)` re-phases the pattern after cropping by `(left=x, top=y)`.
- `RawImage.crops: [usize;4]` is `[top, right, bottom, left]`. Tested Canon CR2s flag top/left only; their LAST two sensor rows also read elevated (~9k vs ~2k ADU) despite `bottom=0` — if a bottom-edge artifact ever appears, suspect unflagged masked rows.
- Icons: `icon::from_name("...").icon().into()` yields an Element; image handles clone cheaply. `widget::icon::from_name(...)` returns a `Named` with `From<Named> for icon::Handle`, so `.leading_icon(widget::icon::from_name(...))` works on buttons.
- **Roll library widgets** (verified against pinned libcosmic): `cosmic::dialog::file_chooser::open::Dialog::new().open_folder()` is async (xdg portal via ashpd), returns `FileResponse` whose `url()` is a `&Url` (path via `to_file_path()`); `Error::Cancelled` covers cancel/portal failure. No public dialog title setter exists in the pinned libcosmic, so the picker is invoked title-less. `widget::search_input(placeholder, value)` (`widget/text_input/input.rs:91`) returns a `TextInput` with `.on_input(Message)` and its own `system-search-symbolic` leading icon; `cosmic::widget::text_input::focus(Id)` reveals and focuses the input once it is mounted. `cosmic::Application` must be `use`d for `Self::APP_ID`/`core_mut()` inside plain `impl AppModel` blocks.
- **iced `widget::shader`** (verified against pinned vendored iced): reach through `cosmic::iced::widget::shader::{Pipeline, Primitive, Program, Shader, Viewport}`. `Shader::new(program).width(Length::Fill).height(Length::Fill)` wraps a `Program` impl. `Program::draw()` produces a `Primitive`; `Pipeline::new()` is called once when iced first sees a new pipeline type; `Primitive::prepare()` runs every frame; `Primitive::render()` runs every frame and uses an `encoder.begin_render_pass(...)` with `LoadOp::Load` (we draw into an already-painted framebuffer). `viewport.physical_{width,height}()` and `bounds.{x,y,width,height} × viewport.scale_factor()` give physical-pixel coordinates for the scissor region.
- **Other iced widget traits** verified for this feature: `MouseArea::new(content).on_double_click(message)` (takes a message value, not a closure); `cosmic::iced::keyboard::listen()` delivers only keyboard events widgets did NOT capture (Status::Ignored) — fine for a global Escape; `Subscription::filter_map` requires NON-CAPTURING closures (`Fn(T) -> Option<A> + Clone`); `cosmic::iced::widget::Stack::with_children([...])` layers children bottom-to-top in push order — used here for thumb-over-shader layering. `Rectangle::center()` (iced/core `rectangle.rs`) and `Cursor::position_in(bounds)` (cursor.rs) power `DetailArea`'s center-relative reporting; `ScrollDelta::{Lines, Pixels}` (iced/core `mouse/event.rs`) is what `on_scroll` receives — a wheel notch gives `Lines { y: ±1 }`, trackpads give `Pixels`.
- **Pure testable helpers**: the zoom/pan messages route through `apply_detail_zoom(zoom, pan, cursor, delta)` and `zoom_about_anchor(z0, z1, pan, cursor)` — no app state, so the pinned-point/round-trip/re-center invariants are unit-tested (`app.rs` tests). `detail_zoom_delta` maps `ScrollDelta → log2` units. The tone model is likewise pure: `curve_remap(contrast, rolloff, shadows, shadow, mid, white)` composes the three pivoted powers into `(ratio, exp)` and `tone_anchors(mono)` measures the 10th/50th/98th-percentile anchors from a histogram — unit-tested for identity-at-defaults, per-control pivot-pinning (`T(mid)=mid`, `T(white)=white`, `T(shadow)=shadow` at identity-others), exact three-power composition (single final clamp, matching the shader), and the degenerate-black guard (`MIN_ANCHOR`). The WGSL itself is validated in `shader::tests::wgsl_source_parses_and_validates` via a `naga` dev-dependency (already in the lockfile through wgpu, so nothing new compiles) — the shader is compiled by wgpu only at first launch, so a parse regression would otherwise surface only at runtime.
- **WGSL shader** (verified with vendored wgpu 28): `vertex_index` builtin provides 0..2; the per-vertex fullscreen-triangle pattern `vec2<f32>((vec2(1u, 2u) + vertex_index) % vec2(6u) < vec2(3u))` is the standard 3-vertex covering-trick; the render pass viewport is set to the primitive's bounds via `pass.set_viewport(...)` so NDC [-1..1] maps to the rect.

## Decisions

ADR-style records (Y-statement form: context → choice → accepted tradeoff). Status **Accepted** unless marked **Superseded**.

- **CPU thumbnails, not GPU** — In the context of edit-aware tiles, we chose CPU re-decodes (rawloader is CPU-only; GPU samplers can't area-average; a GPU tail at ≤384 px buys nothing) over per-tile shader objects, accepting one RAW re-decode per edited file on edit release. (Est. as a limitation, then ratified.) — **Accepted**
- **One shared tone tail + GPU LUT** — In the context of grid/export/detail drift, we single-sourced the tone math in `shader::tone_model` and let the WGSL sample a CPU-built 2048×1 `R16Float` LUT (gamma-domain packed, `curve × 512` pre-scale), accepting a bounded LUT approximation verified by parity tests (≤1e-3 model, ≤5e-4 CPU-vs-GPU simulation). — **Accepted**
- **Three-pivoted-power tone model** — In the context of the D-max/gamma panel being mathematically near-duplicate of exposure, we chose orthogonal contrast/rolloff/shadows powers pivoted at measured image anchors that compose exactly into `ratio·p^exp`, accepting the anchor-measure cost per decode. — **Accepted** (the prior D-max/gamma panel it replaced: **Superseded**)
- **Film choice and base strategy as orthogonal roll-level axes** — In the context of auto modes that previously implied a stock, we made films and bases independent (`BaseMode`→`BaseConfig` mapping unchanged), accepting that legit files choose a film + a base strategy, and that a preset-less roll is non-inverted by default (a deliberate behavior change for legacy rolls). — **Accepted**
- **Half-float `R16Float` texture** — In the context of wgpu 28 not filtering `R32Float`, we store the linear mono as f16 (f32→half in `prepare`), accepting a small quantization on upload. — **Accepted**
- **`2^-EV` sensor-gain flip** — In the context of film presets, the EV gain is flipped in sign at the point EV meets the sensor data (`2^-EV` then density-inversion) so +EV always brightens the positive; `Uniforms.exposure` carries the *gain*, never the raw EV. — **Accepted**
- **Commit-on-release edit cadence** — In the context of per-keystroke RAW re-decode/TOML churn, edits stream live to the RAM + shader and commit exactly once on release/flush (`commit_edit()`), accepting a beat of grid lag behind the detail view. — **Accepted**
- **Detail-overview LRU** — In the context of instant return to recently-viewed frames, we deliberately retain `DETAIL_CACHE_CAPACITY = 40` overviews (~500–670 MB peak) across close/roll switches, accepting a bounded memory cost; native level-up buffers and thumbnail caches are never retained. — **Accepted**
- **Crop mode as a VIM-like modal state** — In the context of the old dimmed-crop overlay toggle, cropping is a real keyboard mode (`c` toggles; white surround + dim mask; Esc exits mode first; crop keys are the only active edits), accepting extra key depth for a cleaner inspect-workspace. — **Accepted** (the "View dimmed crop area" toggle it replaced: **Superseded**)
- **Search is library-only** — In the context of a search that once filtered open-roll frame names, frames are never filtered (the grid always shows every file), accepting that frame lookup stays name-sorted paging. — **Accepted**
- **Flatten-before-average base measurement** — In the context of thumbnail speed, per-class bases/invert anchors are measured at thumbnail scale after the downscale, accepting subtle high-contrast differences vs. the detail render. — **Accepted**
- **Remove roll is non-destructive** — In the context of library management, removing a roll edits config only (files + sidecar untouched), accepting that "delete" still requires manual file removal. — **Accepted**

## Known limitations & next steps

1. **Tone editing is manual, not a stock**: the inversion films are now a picker of real `MonoStock`s (`film::FILM_CHOICES` — Generic, HP5+, Tri-X, FP4+, T-Max 400, Fomapan 100/400, Delta 400, Kentmere 400; each a `(base, d_max, gamma)` profile), but the editing drawer is still a *persisted* manual contrast/rolloff/shadows control rather than per-stock tuned curves. Cast WB stays self-calibrated per image from clear-film plateaus, so margin-less frames rely on the `MIN_PLAUSIBLE_BASE` fallback. **Calibrate-from-a-frame is implemented**: the `AutoSelectedFrame` base mode designates a calibration frame whose measured plateau (`measure_base`) becomes the roll's black point — set via **Edit → Calibrate from this frame** or defaulted to the first frame — so the remaining natural next steps are AsShot-WB priors for margin-less frames and color stocks using `invert_mono`/`measure_base_channels`.
2. **No live refresh**: re-adding a roll re-scans it (the Add Roll tile doubles as a manual refresh), but an open roll's frame list is fixed for the session and the library list has no global rescan button.
3. **Memory unbounded** across many decoded thumbs (no LRU/eviction); ~600 KB each. Detail view holds one oriented mono `Vec<f32>` (~16 MB at 2048×2048 f32) + its GPU texture; zooming past the threshold re-decodes it at native cap (`MAX_TEXTURE_EDGE`), so a >8K sensor transiently holds up to a `long_edge ≤ 8192` mono buffer (~0.5 GB worst case at 8192² f32) until the old buffer is dropped on swap. Live buffers are dropped on close/reselect, but the **detail-overview LRU** (`DETAIL_CACHE_CAPACITY` = 40 × ~12–17 MB ≈ 500–670 MB peak) deliberately retains recently-viewed overviews across close/roll switches — a bounded, intended tradeoff for instant return; native level-up buffers and the thumbnail no-cache policy are unchanged.
4. Decode errors are **logged**, no longer silent (plain `eprintln!` messages matching the codebase's prefix-less "failed to …" style, e.g. `failed to decode <path>: <reason>`, `detail decode failed for <name>: <reason>`): `decode_raw` (thumbnail/cover) and `decode_raw_detail` (detail/export) now thread rawloader's actual error string out of the spawn_blocking call (`Result<DetailDecode, String>`; the two sample-count guard failures carry a message too), `handle_detail_ready` logs the detail failure **once per selection** (`detail_logged_failure`, so wheel-driven re-decodes of the same doomed frame log once, not per notch), preload failures stay quiet (speculative), and `export_one` logs + maps to `()` for its `Result<(), ()>` signature. Non-RAW files are still listed and show the missing icon, and a failed hi-res detail decode still keeps the thumbnail — selecting another file and back retries on demand.
5. **Grain/noise/sharpness**: area-average downscale, luminance collapse, full-res bayer flattening, gentle unsharp mask — plus the post-decode bandwidth savings on the GPU side (one linear texture upload vs. a continuously-re-encoded sRGB image). Remaining stackable option — a denoise pass for very underexposed captures. Capture-side: scans exposed with clear film far left in the histogram waste SNR; exposing so the base sits near saturation (~85–95%) keeps several more stops of detail in dense areas.
6. **Exposure EV precision**: the exposure edit is stored in the manifest and pushed to the GPU uniform as `f32` (`Uniforms.exposure`, `EditData.exposure_ev`), but the slider only spans −3..+3 EV at 0.01 steps (~600 discrete values). Revisit whether the accuracy/width is justified — e.g. a fixed-point or narrower type in the manifest, or whether the shader could take the EV precomputed as f16 via `R16Float`-style storage rather than f32.
7. **GPU vs CPU for thumbnails (decided: CPU)** — evaluated when wiring edit-aware tiles. GPU does NOT help thumbnail *loading*: `rawloader::decode_file` is the dominant cost and is CPU-only; the quality-relevant steps (full-res `flatten_bayer`, linear-light area-average downscale) are intentionally CPU-side because a GPU sampler can't do area-average (mip/bilinear differs), and uploading full-res mono per tile would be ~100 MB/tile. The GPU-able tail (EV multiply + sRGB + quantize at ≤384 px) is trivial CPU, so per-tile shader objects buy nothing. GPU stays where it already pays: the single live detail shader. Consequence taken instead: thumbnails are *re-decoded* CPU-side with the EV baked in on edit release — one RAW re-decode per edited file, background + bounded-concurrent, so the grid updates a beat after the detail view. If instantaneous grid feedback were ever required, the alternative is caching the un-exposed linear mono per tile (~590 KB @ 384 px f32) and re-encoding on edit — rejected now because it breaks the memory-boundedness that the concurrency cap/no-cache currently guarantees (see #3). The accepted flatten-before-average tone swap (per-class bases measured at thumbnail scale after averaging) plus the ≤4-way decode concurrency (transient RAM peak ≈ `MAX_CONCURRENT_THUMBS` decoded full-res buffers) are the current price of the speedup; `CONCURRENT_THUMBS` is a one-line knob.
8. **"Normal positive images" tone-editing support (deferred)** — revisit dates unknown. **Status: the rendering model is symmetric and correct**: film (inverted) and non-inverted positives run one shared tone body (`curve_remap`/`tone_model`/`build_tone_lut`/`render_tail`), anchor to the same {10,50,98}-percentile of the *displayed* positive, and differ only in the per-fragment inversion (`exposure.wgsl` `shade`); pinned by `film_pivots_at_ev0_match_anchors_of_an_inverted_ramp` and `film_pivots_track_the_current_ev`. **Observed gap**: on digital positive RAWs, the shared *linear-space* percentiles sit at different perceptual spots than on density-spread film positives (linear histograms pack toward black), so an identical slider adjustment can read as "shadows/highlights swapped, values flipped" even though no code path is crossed. Two verified sub-points: (a) the shadow pivot is floored (`SHADOW_PIVOT_FLOOR`) identically for both; (b) at identity-everything-else each rotor reduces to a pure `p^exp` lift that raises the whole tail (darks fog) and dims the band above its pivot — *inherent to the single `ratio·p^exp` shape, identical for film*, only hidden there by near-zero blacks. **Candidate directions for the revisit** (not implemented): (i) derive non-inverted anchors in the perceptual γ-domain (`DetailProgram::new` non-inverted branch in `shader.rs` + app-side `pivots_for` in `app.rs` — positive-only, film/render untouched; existing positive edits re-render once) — improves pivot placement but cannot remove the shape-driven fog/peak-dim; (ii) a region-localizing curve shape applied symmetrically to both paths (changes film behavior today's "correct"). Next revisit: measure actual `tone_anchors` + per-slider `ΔT(p)` on a real positive RAW first, then decide between (i), (ii), and storing the anchor domain choice.

## Recent changes

Semantic "unreleased" history; the deep per-change record lives in `git log`. Last major feature round (recent → oldest):

- Split the **film preset** from the **base strategy** into two orthogonal roll-level axes (`base_mode`; `auto-selected-frame` calibration frame + indicator dot); added the remaining film stocks (Fomapan 100/400, Delta 400, Kentmere 400, T-Max 400, Tri-X) and the `Generic` neutral fallback.
- Crop as a **movable/resizable window** in a VIM-like **crop mode** (`c` key + View menu checkbox, dim mask + white surround, Esc exits mode first) replacing the dimmed-crop toggle; display **rotation** (`r`) composed after the crop, persisted and baked.
- **Export**: format dropdown (JPEG 90/82, 16-bit PNG) + overwrite checkbox in the folder dialog, progress ring, atomic writes; **dated rolls** export with `YYYYMMDD-<hash>-<frame>` names and EXIF capture-time stamping.
- Roll metadata: **editable name**, **start/end dates** (ISO, validated), **date-sorted** library (undated first), human-readable localized card dates.
- **Search**: library-only, **debounced** (split draft/applied term), matches **names + dates** via tokenized month/year terms.
- Keyboard: **global help overlay** (`?`), **VIM navigation** (`h/j/k/l`), routing via libcosmic's `keyboard_nav` + a single `modifiers` field; clippy pedantic is clean.